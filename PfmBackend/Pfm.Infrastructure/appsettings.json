{
  "ConnectionStrings": {

    "MSSQLPfmDatabase": "Server=localhost;Database=pfm;Trusted_Connection=True;TrustServerCertificate=True;"

    //Here we can implement different database solutions other than MSSQL like PostgreSQL etc by simply adding more connection strings
  }
}
